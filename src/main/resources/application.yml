server:
    port: 8081
    servlet:
        context-path: /api

spring:
    application:
        name: ims
    profiles:
        active: dev

application:
    resource:
        vendor: vendor
        branch: branch